# Forge Pilot Notes

Instrumentation log for the forge SDD pilot run. One line per event, timestamped.

## Log

- 2026-07-03 ~14:20 — SessionStart hook fired visibly: "forge: no active spec (specs/ exists) — /forge:specify starts one" (clear, actionable).
- 2026-07-03 ~14:20 — SessionStart hook also injected PONYTAIL MODE (lite) — two SessionStart hooks stack without conflict, but ponytail's "shortest diff wins" ethos mildly tensions with a deep-review mission; noted, not blocking.
- 2026-07-03 ~14:21 — Phase 0 (pre-spec investigation) started: 5 subagents launched (architecture, security, correctness, tests, simplicity).
- 2026-07-03 ~14:26 — Reviewer returned: TESTS — verdict: 1 critical / 4 high / 8 medium / 4 low; highly specific (file:line + concrete bug classes, e.g. untested optimistic-lock rotation path tied to the known missing refresh_tokens.version column). Not generic.
- 2026-07-03 ~14:36 — Reviewer returned: CORRECTNESS — verdict: 2 critical / 4 high / 7 medium / 6 low; highly specific. RESOLVES the security-vs-tests tension: token-theft family revocation ROLLS BACK because TokenTheftException (RuntimeException) triggers @Transactional rollback, undoing revokeByFamilyId; AND the OptimisticLockException catch is dead code (wrong exception subtype + fires after tx commit). So security's "rotation clean, no bypass" is WRONG — three independent reviewers converge on the refresh-token subsystem as the deepest risk. Not generic.
- 2026-07-03 ~14:43 — Reviewer returned: ARCHITECTURE — verdict: 0 critical / 2 high / 5 medium / 5 low; highly specific with failure scenarios. Independently corroborates the 403-vs-404 ownership enumeration oracle (also flagged by security-adjacent), the duplicated/unbounded rate limiters, and the session-orchestration-in-controller smell. Not generic.
- 2026-07-06 — Phase 0 synthesis delivered; proposed 7-spec decomposition (A–G), highest-risk first.
- 2026-07-06 — PIPELINE FLAG raised: forge DoD "prove behavior didn't change" fights bug-fix specs (A–D change behavior by design). Proposed split: fix specs prove red→green test + suite stays green; only refactor specs (E,G) use "suite unchanged". Candidate retro proposal for claude-forge DoD template.
- 2026-07-06 — Interview Q1 (spec #1 scope): user chose "Backend trio + tests" (defer multi-tab race to frontend spec D, keep schema fix in-scope). NOT answerable from code — legitimate scoping/judgment decision.
- 2026-07-06 — WORKAROUND: /forge:specify has `disable-model-invocation: true` — cannot be invoked programmatically by the orchestrator. For an agent-driven pilot this blocks the whole "run /forge:specify on the results" instruction. Worked around by reading plugins/forge/skills/specify/SKILL.md + templates/spec.md and following the 10 steps manually. Candidate retro item: forge assumes a human types every phase command; agent-driven runs need either an invocable path or explicit guidance to follow SKILL.md by hand.
- 2026-07-06 — Noticed rules/code drift (relevant to later spec C, not #1): .claude/rules/backend.md mandates AccessDeniedException for ownership violations, but code uses ResourceOwnershipException. Confirms the architecture reviewer's Low finding.
- 2026-07-06 — Discovery: CI (ci.yml:33-46) provisions Postgres 17 + runs `./mvnw clean verify`, but the ONLY DB test (JobTrackerApplicationTests) is @Disabled → CI's Postgres is effectively unused and no test exercises real tx/locking. Root cause of why the 3 critical refresh bugs shipped green.
- 2026-07-06 — Interview Q3 (test infra) is PARTIALLY answerable from code: CI file proves Postgres is available, so the "is a real DB reachable" half is knowable. The remaining half (add Testcontainers dependency vs reuse provisioned Postgres) is a genuine dependency-policy judgment — kept as an interview question. Flagging per instrumentation ask.
- 2026-07-06 — Grounded design in actual code (read RefreshTokenService/RefreshToken/RefreshTokenRepository): all 3 critical bugs confirmed verbatim. Schema-drift mechanism pinned: primitive `int version` → NOT NULL no-default → ddl-auto=update can't ALTER a populated table.
- 2026-07-06 — Interview Q2 (schema fix): user chose @ColumnDefault("0") over Flyway/manual ALTER. NOT answerable from code — dependency/direction judgment.
- 2026-07-06 — Interview Q3 (test infra): user chose Testcontainers Postgres. Half-answerable from code (CI proves Postgres available); dependency choice is judgment.
- 2026-07-06 — HARD GATE cleared: design for spec #1 approved by user (Test C = full populated-table repro). Proceeding to write spec file. No code yet (spec doc only).
- 2026-07-06 — FORGE PHASE TRANSITION: spec #1 status draft (creating specs/0001-refresh-token-hardening/spec.md).
- 2026-07-06 — FORGE PHASE TRANSITION: spec #1 status specified→ (user approved at review gate). spec.md frontmatter flipped draft→specified. Next: /forge:plan.
- 2026-07-06 — /forge:plan also disable-model-invocation — same workaround (drove SKILL.md steps by hand). Confirms the pattern: EVERY forge phase command blocks model invocation, so an agent-driven pipeline must hand-run all of them. Strong retro signal.
- 2026-07-06 — Plan-time discovery: spec Test B assertion ("exactly one InvalidCredentialsException") is FLAKY — losing concurrent rotation can legitimately get InvalidCredentials OR TokenTheft depending on thread timing. Proposing spec relaxation to "one 401-class exception, never a 500". This is a spec refinement caught at plan time (before any frozen plan) — GOOD (the pipeline surfaced it before implement), not a deviation. Will flag at APPROVE gate.
- 2026-07-06 — Plan draft written to scratchpad; self-reviewed; dispatched forge-plan-reviewer.
- 2026-07-06 — FORGE PHASE TRANSITION: spec #1 status planned (user APPROVEd plan at hard gate). plan.md written to specs/0001-refresh-token-hardening/ (frozen); spec frontmatter specified→planned; spec Test B DoD wording relaxed per the flagged change (applied openly at APPROVE, not silent). Plan is now frozen — any implement-phase change is a logged Deviation (Article II).
- 2026-07-06 — Minor pipeline friction: forge plan step 6 says "suggest branch feat/NNNN-<slug>" but the pilot is intentionally on chore/forge-pilot-infra. Left branch mechanics to the user (forge says they belong to the user), noting the tension rather than auto-branching.
- 2026-07-06 — FORGE PHASE TRANSITION: spec #1 status implementing (/forge:implement driven by hand — model-invocation-blocked like the rest). Task-by-task cadence chosen.
- 2026-07-06 — DEVIATION #1 (logged in plan ## Deviations, NOT silent): Testcontainers 2.0.3 (managed by Spring Boot 4.0.1) renamed all modules with `testcontainers-` prefix; plan's `junit-jupiter`/`postgresql` coords → `testcontainers-junit-jupiter`/`testcontainers-postgresql`. Caught at first `./mvnw test`. Packages unchanged, code compiled as-written.
- 2026-07-06 — DEVIATION #2 (logged): plan + plan-reviewer BOTH wrongly assumed src/test/resources/application.properties merges with main. It SHADOWS (same-filename classpath resource). First full-context @SpringBootTest failed on unresolved cors.allowed-origins. Fixed by adding cookie.secure/cors.allowed-origins/ddl-auto=update to test props. NOTABLE: the plan-reviewer explicitly "verified" the inheritance assumption as correct by reasoning from AuthControllerTest — but that's a @WebMvcTest slice, a false analogy. A reviewer's confident verification was WRONG; only runtime caught it. Strong evidence for the pipeline's evidence-before-claims article vs document review.
- 2026-07-06 — DEVIATION #3 (logged): Fix 2 broke RefreshTokenServiceTest:83 (asserted save() called twice; now save once + saveAndFlush once). Updated assertion. The exact brittleness the test-quality reviewer predicted.
- 2026-07-06 — DEVIATION #4 (logged, MOST IMPORTANT): the new *IT classes NEVER RAN under `./mvnw clean verify` — surefire excludes *IT, pom had no failsafe plugin. Silent skip locally AND in CI. Caught ONLY by running the full gauntlet and noticing "Tests run: 191" (unchanged). Added maven-failsafe-plugin. Without this, all the TDD work would have shipped with the integration tests never executing. Headline retro lesson: the gauntlet catches what per-test `-Dtest=` runs hide, because explicit -Dtest overrides surefire's exclude pattern — a false sense of green.
- 2026-07-06 — FORGE PHASE TRANSITION: /forge:verify — DoD re-executed fresh, all PASS (backend BUILD SUCCESS + failsafe 4 IT green; frontend 375/41 + build). Adversarial fresh-context reviewer: "no correctness gaps" — verified all 3 fixes/3 tests/4 deviations present + genuine, confirmed failsafe makes ITs run in CI. SPECIFIC + valuable: it caught a real PROCESS gap — the "frozen" spec.md/plan.md were UNTRACKED (not committed), so Article II's frozen-committed-plan wasn't literally satisfied. Fixed by committing the SDD artifacts. Spec #1 status → verified.
- 2026-07-06 — PIPELINE FINDING (retro candidate): forge never has a step that commits the spec/plan artifacts. specify writes spec.md, plan writes plan.md, but nothing commits them — they linger untracked until a code commit sweeps them or (as here) an adversarial reviewer notices. Article II says "frozen, APPROVEd plan" but the pipeline doesn't enforce it being in git. Proposal: plan step 6 (on APPROVE) and specify step 10 should commit their artifact.
- 2026-07-06 — Task 5 COMPLETE / DoD MET: backend `clean verify` BUILD SUCCESS (surefire 191/0/0, failsafe 4 IT green); frontend lint 0 + 375 tests/41 suites pass + build compiled. Both stacks green, frontend unchanged. Commits: bf17a23, 01e1eb5, 5149538, d5ac9b0, + failsafe/test-fix. Spec #1 implement phase done.
- 2026-07-06 — Task 4 COMPLETE (Fix 3): RED reproduced the EXACT prod drift — "alter table ... add column version integer not null ... contains null values" on the pre-seeded populated legacy table. GREEN after @ColumnDefault("0"). Committed d5ac9b0. The "full populated-table repro" (user's choice over the lazy DDL-string assertion) proved its worth — it caught the real failure mode a fresh-container test never would. Note: Hibernate 7.2 logs the DDL failure as WARN and continues (doesn't hard-fail context), so the test's SELECT is what surfaces red — still a clean red, worth knowing the ddl error is non-fatal at boot.
- 2026-07-06 — Task 3 COMPLETE (Fix 2): RED = loser threw uncaught ObjectOptimisticLockingFailureException (500-class); GREEN after saveAndFlush + Spring-type catch, 3/3 non-flaky + full class 3 tests green. Committed 5149538. The Test B spec-relaxation (accept InvalidCredentials OR TokenTheft) paid off — barrier hit the version-conflict path, but the relaxation makes it robust regardless.
- 2026-07-06 — Task 2 COMPLETE (Fix 1): genuine TDD red→green against real Postgres. RED: "family revocation must persist ... expected <true> but was <false>". GREEN after noRollbackFor: Tests run 1, 0 failures. Committed 01e1eb5. This is the deepest of the 3 critical bugs (security reviewer had called rotation "clean") — now provably fixed with a failing-first test. No IDE-diagnostic noise affected the CLI build (m2e stale cache only).
- 2026-07-06 — Task 1 COMPLETE: RefreshTokenRotationIT context-loads green (1 test, Postgres 17.10). Committed bf17a23. Boxes ticked. Paused for user confirmation (task-by-task).
- 2026-07-06 — Reviewer returned: forge-plan-reviewer — verdict "Issues Found": 1 blocking + 2 advisory, HIGHLY SPECIFIC (verified against every source file, incl. tracing that spring-boot-starter-test doesn't provide @ServiceConnection). Blocking issue was real: I under-specified the Testcontainers deps (missing junit-jupiter + spring-boot-testcontainers) → tests wouldn't compile. Also independently confirmed the config-inheritance assumption and the Test B relaxation soundness. Not generic — caught a genuine build-breaker. Fixed inline: 3 deps, @BeforeEach isolation (I caught a cross-method data-pollution issue the reviewer's count() advisory implied), no-leak assertion.
- 2026-07-06 — Reviewer returned: forge-spec-reviewer — verdict "Issues Found": 2 blocking + 3 advisory, HIGHLY SPECIFIC (keyed to exact line numbers). Both blocking issues were real defects I introduced: (a) DoD's `git stash` red→green can't reproduce "red" at verify time since fixes are committed by then — sharp catch; (b) I contradicted myself on which method owns the try/catch (Problem said rotateRefreshToken, Design said "inside doRotate"). Not generic — this reviewer earned its slot. Fixed all 5 inline (2 blocking + 3 advisory), no re-review per SKILL step 9.
- 2026-07-03 ~14:47 — Reviewer returned: SIMPLICITY — verdict: 0 critical / 3 medium / 4 low; specific with grep-verified "unused" claims. Backend confirmed clean of dead code; findings are frontend orphans from PRs #135-138. Not generic. All 5 Phase 0 reviewers now complete.
- 2026-07-03 ~14:31 — Reviewer returned: SECURITY — verdict: 1 high (SSRF via redirect/DNS-rebind in UrlFetchService), 2 medium, 3 low; IDOR confirmed clean with specifics. Highly specific (attack scenarios per finding). Not generic. NOTE cross-reviewer tension: security says @Version optimistic-lock rotation is "clean, no bypass" but tests reviewer says the version column is MISSING in the real DB — code is correct, prod schema defeats it. Reconcile in merged findings.
